<main>

  <div class="container">
    <div class="row">

      <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
        <h2>新規楽曲投稿</h2>

        <%= form_with model:@artwork, local:true do |f| %>
          <div class="form-group">
            <label for="music_file">ファイル(.mp3 .wav)</label>
            <%= f.file_field :music_file, class:"form-control-file music_file" %>
          </div>
          <div class="form-group">
            <label for="title">タイトル</label>
            <%= f.text_field :title, class:"form-control title" %>
          </div>
          <div class="form-group">
            <label for="body">コメント</label>
            <%= f.text_area :body, class:"form-control body" %>
          </div>
          <div class="form-group">
            <%= f.submit '投稿', class:"btn btn-secondary" %>
          </div>
          <div>
            <%= link_to '戻る', :back, class:"btn btn-secondary text-light" %>
          </div>
          <div>
            <%= f.hidden_field :category, :value => "music" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

</main>